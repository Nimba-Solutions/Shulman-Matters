minimum_cumulusci_version: '3.78.0'
project:
    name: Shulman-Matters
    package:
        name: Shulman-Matters
        api_version: '64.0'
    git:
        default_branch: 'main'
        prefix_feature: 'feature/'
        prefix_beta: 'beta/'
        prefix_release: 'release/'
    source_format: sfdx

tasks:
    robot:
        options:
            suites: robot/Shulman-Matters/tests
            options:
                outputdir: robot/Shulman-Matters/results

    robot_testdoc:
        options:
            path: robot/Shulman-Matters/tests
            output: robot/Shulman-Matters/doc/Shulman-Matters_tests.html

    run_tests:
        options:
            required_org_code_coverage_percent: 75
